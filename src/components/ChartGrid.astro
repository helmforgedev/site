---
import { Search } from 'lucide-astro';

const charts = [
  {
    name: 'Generic',
    slug: 'generic',
    description: 'Multi-purpose chart for Deployments, StatefulSets, Jobs, and CronJobs.',
    color: 'bg-slate-100 text-slate-600',
    letter: 'G',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'MySQL',
    slug: 'mysql',
    description: 'MySQL with standalone and source-replica replication architectures.',
    color: 'bg-sky-100 text-sky-700',
    letter: 'My',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'PostgreSQL',
    slug: 'postgresql',
    description: 'PostgreSQL with standalone and streaming replication support.',
    color: 'bg-indigo-100 text-indigo-700',
    letter: 'Pg',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Redis',
    slug: 'redis',
    description: 'Redis with standalone and Sentinel high-availability modes.',
    color: 'bg-red-100 text-red-600',
    letter: 'Rd',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'MongoDB',
    slug: 'mongodb',
    description: 'MongoDB with standalone and replica set configurations.',
    color: 'bg-green-100 text-green-700',
    letter: 'Mg',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'RabbitMQ',
    slug: 'rabbitmq',
    description: 'RabbitMQ with management UI and clustering support.',
    color: 'bg-orange-100 text-orange-600',
    letter: 'Rb',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'Keycloak',
    slug: 'keycloak',
    description: 'Identity and access management for SSO and federation.',
    color: 'bg-teal-100 text-teal-700',
    letter: 'Kc',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Vaultwarden',
    slug: 'vaultwarden',
    description: 'Bitwarden-compatible password manager with self-hosted simplicity.',
    color: 'bg-violet-100 text-violet-700',
    letter: 'Vw',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Minecraft',
    slug: 'minecraft',
    description: 'Minecraft Java Edition server with backup, monitoring, and mod support.',
    color: 'bg-emerald-100 text-emerald-700',
    letter: 'Mc',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Pi-hole',
    slug: 'pihole',
    icon: 'https://upload.wikimedia.org/wikipedia/commons/1/15/Pi-hole_vector_logo.svg',
    description: 'Network-wide ad blocking with DNS sinkhole and optional recursive DNS.',
    color: 'bg-rose-100 text-rose-700',
    letter: 'Ph',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'WordPress',
    slug: 'wordpress',
    description: 'WordPress CMS with MySQL, S3 backup, and production-friendly defaults.',
    color: 'bg-blue-100 text-blue-700',
    letter: 'Wp',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Strapi',
    slug: 'strapi',
    description: 'Headless CMS with SQLite, PostgreSQL, MySQL, and persistent uploads.',
    color: 'bg-indigo-100 text-indigo-700',
    letter: 'St',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Answer',
    slug: 'answer',
    icon: 'https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons/png/apache-answer.png',
    description: 'Apache Answer Q&A platform with SQL backends and S3 backup.',
    color: 'bg-cyan-100 text-cyan-700',
    letter: 'An',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'n8n',
    slug: 'n8n',
    description: 'Workflow automation with queue mode, SQL backends, and backup support.',
    color: 'bg-amber-100 text-amber-700',
    letter: 'N8',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Komga',
    slug: 'komga',
    description: 'Comics and manga server with OPDS, web reader, and S3 backup.',
    color: 'bg-lime-100 text-lime-700',
    letter: 'Kg',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Guacamole',
    slug: 'guacamole',
    description: 'Remote desktop gateway with RDP, VNC, SSH, and SSO integration.',
    color: 'bg-yellow-100 text-yellow-700',
    letter: 'Gc',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Cloudflared',
    slug: 'cloudflared',
    icon: 'https://svgl.app/library/cloudflare.svg',
    description: 'Cloudflare Tunnel with outbound-only networking and HA options.',
    color: 'bg-orange-100 text-orange-700',
    letter: 'Cf',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'DDNS Updater',
    slug: 'ddns-updater',
    description: 'Dynamic DNS updater with web UI and provider coverage.',
    color: 'bg-purple-100 text-purple-700',
    letter: 'Dd',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'Uptime Kuma',
    slug: 'uptime-kuma',
    description: 'Self-hosted monitoring with status pages and broad notification support.',
    color: 'bg-emerald-100 text-emerald-700',
    letter: 'Uk',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Appwrite',
    slug: 'appwrite',
    description: 'Self-hosted BaaS platform with MariaDB, Redis, and microservices.',
    color: 'bg-pink-100 text-pink-700',
    letter: 'Aw',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'Authelia',
    slug: 'authelia',
    description: 'SSO, MFA, and OpenID Connect for reverse proxies and apps.',
    color: 'bg-blue-100 text-blue-600',
    letter: 'Au',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'AdGuard Home',
    slug: 'adguard-home',
    description: 'DNS ad and tracker blocking with sync and backup features.',
    color: 'bg-green-100 text-green-600',
    letter: 'Ag',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Velero',
    slug: 'velero',
    icon: 'https://raw.githubusercontent.com/cncf/landscape/master/hosted_logos/velero.svg',
    description: 'Kubernetes backup, restore, and migration with object storage.',
    color: 'bg-cyan-100 text-cyan-700',
    letter: 'Ve',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Kafka',
    slug: 'kafka',
    icon: 'https://svgl.app/library/apache_kafka.svg',
    description: 'KRaft single-broker and cluster modes with persistent storage.',
    color: 'bg-orange-100 text-orange-700',
    letter: 'Ka',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'Dolibarr',
    slug: 'dolibarr',
    description: 'ERP and CRM with MySQL, auto-installation, and persistent documents.',
    color: 'bg-fuchsia-100 text-fuchsia-700',
    letter: 'Dl',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'Mosquitto',
    slug: 'mosquitto',
    description: 'MQTT broker with standalone or federated topology and WebSocket support.',
    color: 'bg-cyan-100 text-cyan-700',
    letter: 'Mq',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'Docmost',
    slug: 'docmost',
    description: 'Collaborative wiki with bundled PostgreSQL, Redis, and local or S3 storage.',
    color: 'bg-sky-100 text-sky-700',
    letter: 'Dm',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'Flowise',
    slug: 'flowise',
    description: 'Visual AI orchestration with standalone or scalable queue architecture.',
    color: 'bg-emerald-100 text-emerald-700',
    letter: 'Fl',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'phpMyAdmin',
    slug: 'phpmyadmin',
    description: 'Web-based MySQL and MariaDB administration with multi-server support.',
    color: 'bg-orange-100 text-orange-600',
    letter: 'Pa',
    maturity: 'stable',
    backup: false,
  },
  {
    name: 'Heimdall',
    slug: 'heimdall',
    description: 'Application dashboard for organizing self-hosted services.',
    color: 'bg-sky-100 text-sky-600',
    letter: 'Hd',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Gitea',
    slug: 'gitea',
    description: 'Self-hosted Git service with SQL backends, SSH access, and S3 backup.',
    color: 'bg-green-100 text-green-700',
    letter: 'Gt',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'Homarr',
    slug: 'homarr',
    description: 'Modern dashboard with SQL options, integrations, and S3 backup.',
    color: 'bg-rose-100 text-rose-600',
    letter: 'Hr',
    maturity: 'stable',
    backup: true,
  },
  {
    name: 'MariaDB',
    slug: 'mariadb',
    description: 'MariaDB with standalone and GTID-based replication plus backup support.',
    color: 'bg-teal-100 text-teal-700',
    letter: 'Md',
    maturity: 'stable',
    backup: true,
  },
];

const maturityStyles = {
  stable: 'bg-green-500/10 text-green-400 border-green-500/20',
  beta: 'bg-amber-500/10 text-amber-400 border-amber-500/20',
  alpha: 'bg-red-500/10 text-red-400 border-red-500/20',
} as const;

const chartCount = charts.length;
const backupCount = charts.filter((chart) => chart.backup).length;
const stableCount = charts.filter((chart) => chart.maturity === 'stable').length;
---

<section class="py-16 sm:py-20 relative">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex flex-col gap-8 lg:flex-row lg:items-end lg:justify-between">
      <div class="max-w-2xl">
        <p class="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Catalog</p>
        <h2 class="mt-3 text-3xl sm:text-4xl font-bold tracking-tight text-text-base heading-font">
          A cleaner catalog for common Kubernetes workloads.
        </h2>
        <p class="mt-4 text-base sm:text-lg leading-relaxed text-text-muted">
          Browse application charts with consistent structure, documented values, and production-minded defaults.
        </p>
        
        <!-- Search Input -->
        <div class="mt-8 relative max-w-md group focus-within:ring-0">
          <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
            <Search class="h-5 w-5 text-text-muted transition-colors group-focus-within:text-primary-light" />
          </div>
          <input 
            type="search" 
            id="chart-search" 
            class="block w-full pl-12 pr-4 py-3.5 border border-border rounded-2xl leading-5 bg-bg-surface/80 backdrop-blur-sm text-text-base placeholder-text-muted focus:outline-none focus:ring-2 focus:ring-primary-light focus:border-transparent transition-all sm:text-sm hover:border-border/80 shadow-sm" 
            placeholder="Search charts (e.g. postgres, redis, backup)..." 
          />
        </div>
      </div>

      <div class="grid grid-cols-1 gap-3 sm:grid-cols-3 sm:gap-4 lg:min-w-[320px] mt-8 lg:mt-0">
        <div class="glass-panel rounded-2xl px-4 py-4 text-center">
          <p class="text-3xl font-bold tracking-tight text-text-base">{chartCount}</p>
          <p class="mt-1 text-xs uppercase tracking-[0.16em] text-text-muted">Charts</p>
        </div>
        <div class="glass-panel rounded-2xl px-4 py-4 text-center">
          <p class="text-3xl font-bold tracking-tight text-text-base">{stableCount}</p>
          <p class="mt-1 text-xs uppercase tracking-[0.16em] text-text-muted">Stable</p>
        </div>
        <div class="glass-panel rounded-2xl px-4 py-4 text-center">
          <p class="text-3xl font-bold tracking-tight text-text-base">{backupCount}</p>
          <p class="mt-1 text-xs uppercase tracking-[0.16em] text-text-muted">With S3</p>
        </div>
      </div>
    </div>

    <!-- Empty State -->
    <div id="chart-empty-state" class="hidden mt-12 glass-panel rounded-2xl py-16 text-center border-dashed border-border border-2">
      <svg class="mx-auto h-12 w-12 text-text-muted/50 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
      <div class="text-lg font-bold text-text-base">No charts found</div>
      <p class="mt-2 text-text-muted">Try adjusting your search terms.</p>
    </div>

    <div class="mt-14 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4" id="chart-grid-container">
      {charts.map((chart) => (
        <a
          href={`/docs/charts/${chart.slug}`}
          class="chart-card group relative rounded-3xl border border-border bg-bg-surface/40 p-5 transition-all duration-400 hover:-translate-y-1 hover:border-primary/40 hover:bg-bg-surface/80 hover:shadow-xl hover:shadow-primary/10 overflow-hidden flex flex-col h-full"
          data-name={chart.name.toLowerCase()}
          data-desc={chart.description.toLowerCase()}
        >
          <!-- Glow background -->
          <div class="absolute inset-0 bg-gradient-to-br from-primary/5 via-transparent to-transparent opacity-0 transition-opacity duration-500 group-hover:opacity-100 pointer-events-none"></div>
          
          <div class="relative flex items-start justify-between gap-3">
            <div class="flex items-center gap-3 min-w-0 flex-1">
              <div class={`flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl text-sm font-bold shadow-inner ${chart.color} opacity-90 group-hover:opacity-100 transition-all duration-300 group-hover:scale-110 bg-white/5 backdrop-blur-md border border-white/10 overflow-hidden`}>
                <img 
                  src={chart.icon || `https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons/png/${chart.slug !== 'generic' ? chart.slug : 'kubernetes'}.png`} 
                  alt={`${chart.name} icon`}
                  class="w-6 h-6 object-contain drop-shadow-md"
                  onerror="this.onerror=null; this.src='/chart-placeholder.svg';"
                />
              </div>
              <div class="min-w-0 flex-1 pr-1">
                <div class="truncate text-base font-bold text-text-base transition-colors group-hover:text-primary-light" title={chart.name}>
                  {chart.name}
                </div>
                <div class="flex flex-wrap items-center gap-1.5 mt-1">
                  <span class={`inline-flex rounded-full border px-1.5 py-0.5 text-[9px] font-bold uppercase tracking-[0.16em] ${maturityStyles[chart.maturity]}`}>
                    {chart.maturity}
                  </span>
                  {chart.backup && (
                    <span class="inline-flex rounded-full border border-sky-500/30 bg-sky-500/10 px-1.5 py-0.5 text-[9px] font-bold uppercase tracking-[0.16em] text-sky-400">
                      S3 backup
                    </span>
                  )}
                </div>
              </div>
            </div>

            <svg class="mt-0.5 h-4 w-4 shrink-0 text-text-muted transition-transform group-hover:translate-x-1 group-hover:text-primary-light" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </div>

          <p class="mt-3.5 text-xs leading-relaxed line-clamp-2 text-text-muted flex-grow">
            {chart.description}
          </p>
        </a>
      ))}
    </div>
  </div>
</section>

<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const searchInput = document.getElementById('chart-search');
    const chartCards = document.querySelectorAll('.chart-card');
    const emptyState = document.getElementById('chart-empty-state');
    
    if (searchInput) {
      searchInput.addEventListener('input', (e) => {
        const term = e.target.value.toLowerCase().trim();
        let visibleCount = 0;
        
        chartCards.forEach((card) => {
          const name = card.getAttribute('data-name');
          const desc = card.getAttribute('data-desc');
          
          if (name.includes(term) || desc.includes(term)) {
            card.style.display = '';
            visibleCount++;
          } else {
            card.style.display = 'none';
          }
        });

        if (visibleCount === 0) {
          emptyState.classList.remove('hidden');
        } else {
          emptyState.classList.add('hidden');
        }
      });
    }
  });
</script>
